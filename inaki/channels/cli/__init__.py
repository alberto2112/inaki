"""Canal CLI: el chat interactivo del operador contra el daemon (vía REST).

``runner`` es el REPL; ``ports`` el contrato ``IDaemonClient`` que implementa el
cliente HTTP de ``inaki.cli``; ``config`` la sección ``channels.cli``.
"""
