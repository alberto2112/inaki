"""Canal Telegram — el primer canal vertical de Inaki.

Adentro vive TODO lo que es de Telegram y nada más: el bot (``bot.py`` y sus
handlers de comandos, grupos y media), el egress ``outbound.py`` (el BORDE del
transporte: formateo, historial y emisión de broadcast), las tools propias
(``tools/``), el transporte de broadcast entre instancias (``broadcast/``), el
registro de ficheros recibidos (``files/``) y su sección de config con
migraciones y validaciones (``config.py``).
"""
